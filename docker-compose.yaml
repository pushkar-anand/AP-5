services:
  ap5:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/config.yaml:ro
      - ap5-data:/data
    environment:
      AP5_DATA_DIR: /data
    command: ["serve", "--config", "/config.yaml", "--data", "/data"]

volumes:
  ap5-data:
